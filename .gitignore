# macOS
.DS_Store

# Build output — generado en CI por node build.js
dist/

# Wrangler (auto-generado en CI, no commitear)
.wrangler/
wrangler.jsonc

# El manifest se genera automáticamente en el build
# (node build.js en CI, node generate-manifest.js en local)
images/manifest.json
